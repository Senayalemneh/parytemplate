import { Box } from "@mantine/core";

const AdminDashboard = () => {
  return (
    <Box>
      <div>This is the page to register the Sub-city!</div>
      <div>This is the page to register the Sub-city!</div>
      <div>This is the page to register the Sub-city!</div>
    </Box>
  );
};

export default AdminDashboard;
